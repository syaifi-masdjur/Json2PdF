<UserControl x:Class="Json2PdF.Window.Views.RichTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Json2PdF.Window.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <RichTextBox>
            <FlowDocument PageWidth="2048" FontFamily="{Binding ActiveFontFamily, Mode=TwoWay}"  FontSize="{Binding ActiveFontSize, Mode=TwoWay}" FontStyle="{Binding ActiveFontStyle, Mode=TwoWay}">
                
            </FlowDocument>
        </RichTextBox>
    </DockPanel>
</UserControl>
